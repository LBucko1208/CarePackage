" Enable line numbers
set number

" Set tab to 4 spaces
set tabstop=4
set shiftwidth=4
set expandtab

" Enable autoindent
set autoindent

" Enable backspace to work as it should
set backspace=indent,eol,start

" Set default font (comment out if not wanted)
set guifont=Consolas:h10:cANSI:qDRAFT

" Bind <Space> to insert new line in normal mode
noremap <Space> o<Esc>

" Bind keys to insert a matching pair
inoremap ' ''<Left>
inoremap " ""<Left>
inoremap { {}<Left>
inoremap {<CR> {<CR>}<Esc>O
inoremap [ []<Left>
inoremap ( ()<Left>
inoremap (<CR> (<CR>)<Esc>O
""inoremap < <><Left>

" Bind keys to insert speed up adding empty key pair
inoremap '' ''
inoremap "" ""
inoremap {} {}
inoremap [] []
inoremap () ()
""inoremap <> <>

" Bind keys to insert a single when two are input
inoremap '' '
inoremap "" "
inoremap {{ {
inoremap [[ [
inoremap (( (
""inoremap << <

